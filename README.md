# RegexQL (mini compiler-style project)

## Overview
RegexQL converts simple English-like queries into regular expressions and finds matches in sample text.
It demonstrates compiler concepts: lexer, parser, translator, executor.

## Build
```bash
make
./regexql
